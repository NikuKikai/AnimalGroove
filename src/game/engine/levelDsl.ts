import type { LevelDefinition } from "../types";

/** Returns the provided level definition without additional transformation. */
export function defineLevel(level: LevelDefinition): LevelDefinition {
  return level;
}
